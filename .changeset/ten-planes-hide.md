---
"@sittari/payload-path-field": minor
---

Add an internal route registry for published path lookup and lifecycle synchronization. Simplify `findDocumentByPath` to accept only `path` and `overrideAccess`.
