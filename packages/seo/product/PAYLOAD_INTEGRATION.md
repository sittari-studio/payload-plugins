# Payload integration product contract

See [Payload integration](../docs/PAYLOAD_INTEGRATION.md). Settings own global
and collection schema templates; documents own stable references and localized
patches. Template deletion cascades to document instances or global overrides
in the same Payload transaction. Multiple defaults seed only new documents.
